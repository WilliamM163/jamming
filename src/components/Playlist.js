import React from 'react';

import styles from '../styles/App.module.css'

function Playlist() {
    return (
        <div className={styles.accent_container} style={{'marginBottom': 15}}>
            <p>Playlist</p>
        </div>
    );
}

export default Playlist;